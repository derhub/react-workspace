{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "version": "0.0.1",
  "scripts": {
    "build": "lerna run start --scope={@space/core,@space/web}",
    "clean": "lerna clean",
    "ls": "lerna ls",
    "bootstrap": "lerna bootstrap"
  },
  "name": "react-workspace",
  "devDependencies": {
    "lerna": "^3.16.4"
  }
}